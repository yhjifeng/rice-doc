## Frequently Asked Questions

#### 1. How do I teleport across services?

````
First you need to install this plugin in all the landmark enabled servers
Then change the following configuration in config.yml

# Cross-services transfer logo - each server must be the same as the one configured in bc
serverName: "server"

Note: Be sure to configure this correctly before you start creating landmarks, and do not modify it!
If the configuration is wrong, you need to use the /plw clear command to clear the wrong landmarks

If the configuration is wrong, you need to use the /plw command to clear the wrong landmark.